{
  "name": "authless-fastmcp-server",
  "version": "1.0.0",
  "description": "A simple MCP server with weather tool deployed on Cloudflare Workers",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "python-dev": "uv run python -m authless_fastmcp_server.main"
  },
  "keywords": ["mcp", "cloudflare", "workers", "weather"],
  "author": "Gabriel",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.78.12",
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.20241205.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
